namespace Legal.Localization;

/// <summary>Continent a language is grouped under in the pickers. Each language belongs to exactly
/// one: the continent it originates from, so no language is listed twice.</summary>
public enum LanguageContinent
{
    Europe,
    Asia,
    Africa,
    America,
    Oceania
}
